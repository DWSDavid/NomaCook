# CLAUDE.md — NomaChef(诺妈)项目上下文
> **2026-07-23 架构更新:** 当前硬件主线已改为 DFRobot ESP32-S3 AI CAM (DFR1154) + 后端 AI。关于硬件、端到端架构、语音视觉同步、RAG 和数据集的最新决策，以 [`docs/NOMACHEF-TECHNICAL-SPEC.md`](docs/NOMACHEF-TECHNICAL-SPEC.md) 为准。本文件后文的 Raspberry Pi 5 描述仅保留为历史上下文。
> **产品名:NomaChef(英)/ 诺妈(中)。** "No Mom Cook" + The Mom Test 彩蛋;中文取"承诺妈妈"正向反转。
> Slogan:「妈,我会做饭了。」/ "Mom, I cooked." 成品分享功能名:「喏,妈」。梗弹药:「妈见夸」。
> 仓库目录:`~/Documents/NomaChef`
> 这份文件是项目的完整上下文。放在仓库根目录,Claude Code 会自动读取。
> 读完你应该能直接开始搭第一个可跑通的原型。
---
## 0. 一句话总纲
**一个胸前佩戴的实时"流程教练":摄像头看着你做菜,语音一步步指导,并核对每步做对没有。核心设计原则——看结果状态,不看动作过程。**
做菜是切入点(wedge),真正在建的是"任何双手物理任务的第一人称流程辅助引擎"。换SOP库即可迁移到:视障者日常辅助、老人独立生活、实验室/临床流程、拼装维修、动手STEM教育。
---
## 1. 系统架构(5模块)
```
菜谱来源 ──①解析──▶ SOP JSON ──②下发──▶ 胸前采集 ──③三层感知──▶ 状态引擎 ──④语音/屏幕──▶ 用户
（链接/截图）      （带检查点）    （Pi+摄像头）   【CV核心】     （打分推进）    （对话指导）
                                                      │
                                                 过程数据回写 ──▶ Supabase（也是具身智能数据集雏形）
```
**难点100%在③三层感知。** 其余4个模块是现成API/工具编排,一天出活。
---
## 2. ③ 三层感知(CV核心,最该花时间)
设计原则:**便宜信号连续跑,贵模型按需问。** 不做实时动作识别(需自训数据集,必死),用多个免训练粗糙信号叠加打分,替代一个精确信号。
### 第一层 · 连续感知(本地/笔记本,每秒多次,近零成本)
| 信号 | 工具 | 干什么 |
|---|---|---|
| 物体检测 | **YOLO-World**(开放词汇) | 每步把该步物料清单当文字prompt,框出"酱油瓶/锅/铲"在不在、在哪。零训练。 |
| 手部 | **MediaPipe Hands** | 21关节点:手在哪、拿没拿东西、靠近哪个物体 |
| 音频事件 | **YAMNet** 或音量+频率规则 | 切菜"笃笃"、下锅"刺啦"、水开"咕嘟"。极便宜极可靠。 |
| 区域颜色 | **OpenCV** HSV直方图 | 锁定锅ROI后统计颜色,加酱油=颜色向深褐移动 |
**关键:先锁ROI再看变化。** 每帧先用 YOLO-World 定位锅/案板,只比"这一秒的锅"和"十秒前的锅",人怎么走动都不影响。锁定后用 OpenCV tracker(CSRT/KCF)跟住,检测只需偶尔重定位。遮挡时用上帧位置外推,丢>3秒再报。
### 第二层 · 周期性理解(云端VLM,每10-15秒 + 事件触发)
"conversational"和"主动提醒"的引擎。慢速持续巡检:当前帧(叠加第一层检测框)发给VLM,固定问JSON四件事:
1. 当前步骤进度?(未开始/进行中/疑似完成)
2. 有无操作错误或风险?(火太大/要糊了/切太厚)
3. 下一步材料是否已出现在画面?
4. 有什么值得主动说的?(一句话或"无")
第4项让系统主动开口("火可以调小了""蒜还没剥,现在有空可以先剥")。成本:一小时≈250次调用,几毛钱。只问封闭问题(yes/no/unclear),不问开放的"用户在干嘛"。
### 第三层 · 对话(流式语音,用户发起)
用户随时问"这样切对吗""还炒多久",带当前画面+步骤上下文回答。
**坑:实时语音会话通常有单次时长上限(约10分钟),做菜会超时。** 所以状态必须存在我们自己的引擎里,会话断了重连时把"当前第几步+SOP"作为新会话初始上下文注入,用户无感。**这是demo现场最易爆炸的点,必须提前测重连。**
---
## 3. 模型选型(重要:每层用对工具)
| 层 | 任务 | 首选 | 为什么 / 备胎 |
|---|---|---|---|
| 锁ROI/每帧物体 | 检测(非LLM!) | **YOLO-World** | LLM每帧跑=延时+成本爆炸。备胎 Grounding DINO |
| 第二层验证 | 封闭VQA | **Gemini Flash** | 快、便宜、原生吃图/视频。备胎 **GLM-4.6V-Flash**(想要便宜/本地/隐私故事,9B可自部署,MIT license) |
| 第三层对话 | 流式语音 | **Gemini Live** | 三家里唯一有"语音进+语音出+可打断",做菜双手占用的命根子。几乎无替代 |
| 菜谱→SOP | 文本/多模态解析 | **Claude**(图文,结构化稳)/ **Gemini**(视频,原生吃视频) | DeepSeek只碰文本(它的视觉API状态混乱,别赌);文本解析可用它省钱 |
**一句话:Gemini负责"看得懂+能对话",YOLO-World负责"看得快",GLM是想走本地/开源时的Gemini替身,DeepSeek只碰文本。**
---
## 4. 状态引擎:如何判断"完成一步"
**证据累积打分制**,不用单一触发。以"倒酱油"为例:
| 证据 | 来源 | 分值 |
|---|---|---|
| 手持酱油瓶在锅上方 | YOLO-World + MediaPipe | +0.4 |
| 锅内颜色变深 | ROI颜色统计 | +0.3 |
| VLM判"疑似完成" | Gemini Flash | +0.3 |
| 用户说"好了" | 语音 | 直接完成 |
累计 **≥0.7 且连续2次达标** → 完成,播报下一步。卡在0.4-0.7超30秒 → 系统问"酱油加好了吗?"(把不确定变自然对话)。
**为什么有效:** 单信号必误判(路过灶台酱油瓶也在画面),多个独立信号交集,误判率相乘,降一两个数量级。这就是不用自训模型也能跑准的原因,也是pitch里"别人抄不走的壁垒"。
**三个具体场景:**
- 加没加酱油 → 上面打分表
- 切得咋样 → "是否在切"=音频+持刀姿态(连续);"切得好不好"=等切菜声停(天然检查时机)抓案板单帧问VLM"粗细均匀吗"。切工是静态可判,不看过程。
- 材料在不在 → 每步开始 YOLO-World 扫物料清单,缺的报"还没看到葱,在手边吗?"
---
## 5. SOP JSON Schema(所有模块共用的合同)
```json
{
  "dish": "蛋炒饭",
  "ingredients": [{ "name": "鸡蛋", "amount": "2个" }],
  "steps": [
    {
      "step": 3,
      "instruction": "倒入打散的鸡蛋，翻炒至凝固成块",
      "objects_involved": ["bowl", "wok", "spatula"],
      "completion_check": "锅中鸡蛋是否已凝固成金黄色块状?",
      "est_duration_sec": 90,
      "check_policy": "timer_then_visual",
      "tips": ["油热到微微冒烟再下蛋"],
      "failure_modes": ["蛋发褐色→提示立即盛出"]
    }
  ]
}
```
**`completion_check` 是CV层的接口**——只写**静态可判定**的条件("锅中蛋成金黄块状"),绝不写动作("倒酱油")。整个产品的智能浓缩在这个字段的生成质量上。
---
## 6. 其余模块(编排,非难点)
- **① 菜谱抓取:** 首选 MediaCrawler(Playwright驱动真实浏览器,免逆向,扫码复用cookie)。**demo主路用备胎:** 用户分享链接/截图→直接丢VLM解析(零风控、演示直观)。爬虫放pitch讲"未来全网聚合"。
- **① 菜谱→SOP:** 一次LLM调用,输入帖子图文(+视频抽帧),输出上面的JSON。
- **③ 数据库:** Supabase(Postgres + pgvector向量 + 文件存储 + 登录,一站式,免费档够)。四表:`recipes`(SOP+菜名向量)/`sessions`(谁/哪道菜/成品照)/`step_events`(每步时间戳+验证结果+关键帧)/`users`(自带auth)。
  - **做菜过程不需要RAG**,当前SOP塞prompt即可(几KB);RAG只用于菜谱库搜索("想吃辣的下饭菜"→菜名向量检索)。
  - `step_events` 就是具身智能数据集雏形:每行=带时间戳/画面/语义标签的第一人称操作记录。
- **④ App↔硬件:** 不做蓝牙。都连同一后端传话:App点"开始"→后端建session→Pi(挂WebSocket)收SOP→开跑→事件回传→App订阅同session实时显示(顺便是便携屏画面,一份代码两用)。**备胎:** WebSocket不稳退化成HTTP每2秒轮询。
---
## 7. 技术栈总表
| 环节 | 首选 | 备胎 |
|---|---|---|
| 物体检测/ROI | YOLO-World | Grounding DINO |
| 手部 | MediaPipe Hands | — |
| 音频事件 | YAMNet / 规则 | 不做,纯视觉降级 |
| 区域变化 | OpenCV HSV | — |
| 步骤验证 | Gemini Flash | GLM-4.6V-Flash |
| 语音对话 | Gemini Live | Whisper + ElevenLabs 拼装 |
| 菜谱→SOP | Claude(图文)/ Gemini(视频) | DeepSeek(纯文本部分) |
| 抓取 | MediaCrawler | 分享链接/截图→VLM(demo主路) |
| 数据库 | Supabase | Firebase → SQLite+JSON |
| 后端 | FastAPI(Python) | Node/Express |
| App | Next.js网页(手机浏览器开) | 纯HTML |
| 硬件↔云 | WebSocket | HTTP轮询 |
---
## 8. 硬件
- **摄像头:Raspberry Pi Camera Module 3 Wide**(~$35,120°广角、自动对焦、CSI直连不占CPU)。配22-pin转15-pin排线。
- **必带备胎:** USB广角摄像头(CSI排线现场会折断)。
- **锁焦:** libcamera 手动锁焦到胸前→灶台约50-70cm(`AfMode=Manual`),否则蒸汽/手部移动让自动对焦拉风箱。
- demo阶段胸前"box"可空壳,算力全在笔记本+云。Pi只采集+推流(MJPEG over HTTP,~200ms够用)。
- **音频:** 骨传导耳机(物理消除回声)+领夹麦(抗噪);评委听声用屏幕旁监听音箱放副本,不进麦克风链路。
- 便携屏(可视化AI看到的检测框+当前步骤,强烈建议)、手机热点(现场WiFi不稳=demo第一死因)、电磁炉、同道菜×5份。
---
## 9. Hackathon优先级
**P0 — 没有就死(第一天必通):**
1. 摄像头→笔记本→Gemini Live 视频+语音闭环
2. 一道菜(蛋炒饭级)手写状态机 + 逐步语音指导
3. 胸带佩戴稳定工作20分钟
4. 电磁炉现场真炒
**P1 — Wow moment(第二天上午):**
5. 小红书截图→自动生成SOP(demo开场杀手镜头)
6. 便携屏可视化:实时检测框+当前步骤高亮
7. 成品拍照→自动生成分享卡片(收尾镜头)
8. Pi5+外壳,让硬件像产品
**P2 — 只出现在pitch slides,一行代码不写:**
9. 全网菜谱聚合爬虫 · 10. 数据脱敏管线+具身智能数据市场 · 11. 社群/捆绑销售 · 12. 原生App
**demo选菜:** 蛋炒饭/pancake(每步颜色形态都变)。避开炖煮(半小时没变化=死)。
---
## 10. 立即要做的(建议开发顺序)
1. **[最高杠杆] 先录一段自己做菜的完整第一人称视频** —— 它就是测试集,拿它离线调 `completion_check` prompt 比 live 调试快10倍。
2. Pi + CM3 Wide 点亮、锁焦、MJPEG推流到笔记本。
3. Gemini Live hello world:纯语音对话闭环。
4. 定义 SOP schema(见§5)+ 手写一道菜的SOP JSON。
5. 状态引擎打分逻辑(§4)+ 三层串联,宿舍灶台跑完整一道菜。
6. Supabase 四表 + Next.js网页遥控/显示。
7. Live会话重连、断网降级、胸戴物理角度。
8. 现场排练×3,烧掉三份食材。
**并行分工(4人):** 一人测YOLO-World词表 · 一人调巡检prompt · 一人写打分引擎+状态机 · 一人搭Supabase+网页。
---
## 11. 竞品/壁垒(pitch用)
- 智能家电(June/Chef iQ/Thermomix):相机只看锅内,看不见案板和手 → 你的结构性护城河。
- 手机AR应用(ARChef):只"识别食材→推荐菜谱",不做过程指导(手机视角看不清手)。
- 智能眼镜(Ray-Ban Meta,长期对手):眼镜看眼睛看的地方(晃),胸前看手做事的地方(稳)→ 专属人体工学论据。
- 学术界:"第一人称流程辅助"是活跃研究领域,证明架构正确;"验证结果状态而非执行过程"是2025-2026前沿方向。
- **壁垒一句话:** 不是调了个API,而是设计了一套**多信号融合的第一人称状态估计系统**,全系统零自训模型,四人两天能做完,而编排逻辑别人抄不走。
